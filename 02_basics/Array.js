//Array in depth

const MyArray =[0,1,2,3,4,5]
console.log(MyArray[1])

let MyArray2 = new Array("akash","adarsh",)
console.log(MyArray2[0])

//Array methods

MyArray.push(6)
console.log(MyArray)// Push adds element to the end of the array.it will add the element
MyArray.pop(6)
console.log(MyArray)// Pop removes the last element of the array


MyArray.unshift(9)
console.log(MyArray)// it will add the element to the start of the array like 9,0,1,2,3 etc
MyArray.shift()
console.log(MyArray)// it will remove the first element of the array= like 1.2.3,4 will will remove 0 